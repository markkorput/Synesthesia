// Generated by CoffeeScript 1.6.3
(function() {
  var OrientCmsItemView, OrientCmsView, OrientGlobalTargetControlView, _ref, _ref1, _ref2,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  this.OrientCms = (function() {
    function OrientCms(opts) {
      var targetControlModel;
      this.options = opts || {};
      targetControlModel = new Backbone.Model({
        orientationValue: 0
      });
      this.targetControlView = new OrientGlobalTargetControlView({
        model: targetControlModel
      });
      document.body.appendChild(this.targetControlView.el);
      this.view = new OrientCmsView({
        collection: opts.clients
      });
      document.body.appendChild(this.view.el);
      opts.clients.on('change:highlighted', function(model, value, obj) {
        if (value === true) {
          return this.each(function(m) {
            if (m.cid !== model.cid) {
              return m.set({
                highlighted: false
              });
            }
          });
        }
      });
    }

    return OrientCms;

  })();

  OrientGlobalTargetControlView = (function(_super) {
    __extends(OrientGlobalTargetControlView, _super);

    function OrientGlobalTargetControlView() {
      _ref = OrientGlobalTargetControlView.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    OrientGlobalTargetControlView.prototype.tgName = 'div';

    OrientGlobalTargetControlView.prototype.className = 'orient-global-target-control-view';

    OrientGlobalTargetControlView.prototype.events = {
      'mousemove #slider': 'sliderChanged'
    };

    OrientGlobalTargetControlView.prototype.initialize = function() {
      this.$el.append('<span id="display">0</span><input type="range" id="slider" value="0" min="0" max="360">');
      if (this.model) {
        this.updateValues();
        return this.model.on('change', this.updateValues, this);
      }
    };

    OrientGlobalTargetControlView.prototype.updateValues = function() {
      if (!this.model) {
        return;
      }
      this.$el.find('#slider').val(this.model.get('orientationValue'));
      return this.$el.find('#display').text(this.model.get('orientationValue'));
    };

    OrientGlobalTargetControlView.prototype.sliderChanged = function(event) {
      if (!this.model) {
        return;
      }
      return this.model.set({
        orientationValue: $(event.target).val()
      });
    };

    return OrientGlobalTargetControlView;

  })(Backbone.View);

  OrientCmsView = (function(_super) {
    __extends(OrientCmsView, _super);

    function OrientCmsView() {
      _ref1 = OrientCmsView.__super__.constructor.apply(this, arguments);
      return _ref1;
    }

    OrientCmsView.prototype.tagName = 'div';

    OrientCmsView.prototype.className = 'orient-cms-view';

    OrientCmsView.prototype.initialize = function() {
      var _this = this;
      if (this.collection) {
        this.collection.each(function(model) {
          return _this._addItemView(model);
        });
        return this.collection.on('add', function(model) {
          return _this._addItemView(model);
        });
      }
    };

    OrientCmsView.prototype._addItemView = function(model) {
      var view;
      view = new OrientCmsItemView({
        model: model
      });
      model.cmsView = view;
      return this.$el.append(view.el);
    };

    return OrientCmsView;

  })(Backbone.View);

  OrientCmsItemView = (function(_super) {
    __extends(OrientCmsItemView, _super);

    function OrientCmsItemView() {
      _ref2 = OrientCmsItemView.__super__.constructor.apply(this, arguments);
      return _ref2;
    }

    OrientCmsItemView.prototype.tagName = 'div';

    OrientCmsItemView.prototype.className = 'orient-cms-item-view';

    OrientCmsItemView.prototype.events = {
      'mouseover': '_onHover'
    };

    OrientCmsItemView.prototype.initialize = function() {
      this.$el.append('<p id="orientation"></p>');
      this.$el.append('<p id="position"></p>');
      this.updateValues();
      if (this.model) {
        return this.model.on('change', this.updateValues, this);
      }
    };

    OrientCmsItemView.prototype.updateValues = function() {
      if (!this.model) {
        return;
      }
      this.$el.find('p#orientation').text('Orientation: ' + _.map(this.model.get('orientation').toArray(), function(str) {
        return str.toString().substring(0, 5);
      }).join(', '));
      this.$el.find('p#position').text('Position: ' + _.map(this.model.get('position').toArray(), function(str) {
        return str.toString().substring(0, 5);
      }).join(', '));
      if (this.model.get('highlighted') === true) {
        return this.$el.addClass('highlighted');
      } else {
        return this.$el.removeClass('highlighted');
      }
    };

    OrientCmsItemView.prototype._onHover = function(evt) {
      return this.model.set({
        highlighted: true
      });
    };

    return OrientCmsItemView;

  })(Backbone.View);

}).call(this);
